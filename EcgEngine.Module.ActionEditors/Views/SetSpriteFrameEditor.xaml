<UserControl x:Class="EcgEngine.Module.ActionEditors.Views.SetSpriteFrameEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/" 
             xmlns:viewmodels="clr-namespace:EcgEngine.Module.ActionEditors.ViewModels" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance Type=viewmodels:SetSpriteFrameEditorViewModel}"             
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <StackPanel
            Width="450"
            Height="250"
            HorizontalAlignment="Center">
        <TextBlock 
                FontSize="24" HorizontalAlignment="Center"
                Text="PlayAnimation"  />
        <TextBlock 
                Margin="0 30 0 15"
                HorizontalAlignment="Center"
                FontSize="18"
                Text="Animation Name" />

        <TextBox 
                HorizontalContentAlignment="Center"
                FontSize="16"
                Text="{Binding SpriteName}" Width="100" Height="28"/>
    </StackPanel>
</UserControl>
