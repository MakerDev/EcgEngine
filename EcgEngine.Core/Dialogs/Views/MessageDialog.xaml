<UserControl x:Class="EcgEngine.Core.Dialogs.Views.MessageDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/" 
             xmlns:viewmodels="clr-namespace:EcgEngine.Core.Dialogs.ViewModels" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance Type=viewmodels:MessageDialogViewModel}"             
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid 
        Width="350" Height="200">
        <TextBlock 
            Margin="30 30"
            d:Text="This is Message"
            HorizontalAlignment="Center"          
            TextWrapping="Wrap" TextAlignment="Center"
            Text="{Binding Message}" FontSize="28"  />


        <StackPanel 
            Margin="0 100 0 0"
            Orientation="Horizontal"
            HorizontalAlignment="Center">
            <Button
                Command="{Binding CloseCommand}"
                Content="Ok" Width="100" Height="25" />
        </StackPanel>
    </Grid>
</UserControl>
